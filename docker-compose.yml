version: '3'

services:
  app:
    build: .
    ports:
      - 80:80
    depends_on:
      - db

  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: phpmotors
      MYSQL_USER: iClient
      MYSQL_PASSWORD: 8J**EG]Cs0YxI3pN
